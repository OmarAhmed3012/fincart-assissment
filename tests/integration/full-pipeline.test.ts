import { describe, it } from 'vitest';

describe('full pipeline integration', () => {
  it.todo(
    'API ingest to worker terminal-state verification requires Redis/Mongo and both services running via Docker Compose',
  );
});
