{
  "name": "@fincart/gateway-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --import tsx src/main.ts",
    "build": "npm run build -w @fincart/shared && tsc -p tsconfig.json",
    "test": "npm run build -w @fincart/shared && vitest run"
  },
  "dependencies": {
    "@fincart/shared": "*",
    "bullmq": "^5.41.0",
    "ioredis": "^5.5.0",
    "mongodb": "^6.13.0",
    "pino": "^9.6.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "vitest": "^3.0.8"
  }
}