{
  "name": "fincart",
  "private": true,
  "workspaces": [
    "gateway-api",
    "gateway-worker",
    "packages/shared"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @fincart/gateway-api",
    "dev:worker": "npm run dev -w @fincart/gateway-worker",
    "dev": "echo Run dev:api and dev:worker in separate terminals",
    "build:shared": "npm run build -w @fincart/shared",
    "lint": "eslint --no-error-on-unmatched-pattern --ext .ts gateway-api/src gateway-api/tests gateway-worker/src gateway-worker/tests packages/shared/src packages/shared/tests",
    "test": "npm run test -w @fincart/gateway-api && npm run test -w @fincart/gateway-worker",
    "test:api": "npm run test -w @fincart/gateway-api",
    "test:worker": "npm run test -w @fincart/gateway-worker",
    "build": "npm run build -w @fincart/shared && npm run build -w @fincart/gateway-api && npm run build -w @fincart/gateway-worker",
    "format": "prettier --write .",
    "load-test": "npx tsx scripts/load-test/run-load-test.ts",
    "db:init-indexes": "npx tsx scripts/db/init-indexes.ts",
    "dlq:replay": "npx tsx scripts/dlq/replay-dlq.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.25.0",
    "@typescript-eslint/parser": "^8.25.0",
    "@types/node": "^22.13.10",
    "eslint": "^9.21.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  }
}
